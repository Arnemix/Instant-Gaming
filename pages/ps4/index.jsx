import Loader from "@/components/loader/Loader";
import React from "react";

const page = () => {
    return (
        <div>
            <h1>Salut</h1>
            <Loader />
        </div>
    );
};

export default page;
